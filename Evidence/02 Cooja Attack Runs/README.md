# Cooja Attack Runs

Each attack folder contains its Cooja configurations, attack source code, and raw logs for five attack and five control runs. The `Raw Runs` folders contain the Cooja radio trace, test log, and console log for each fixed seed.
