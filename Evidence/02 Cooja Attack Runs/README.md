# Cooja Attack Runs

Each attack folder contains ten Cooja configurations, its `code` directory, and raw logs for five attack and five control runs. `Raw Runs` contains the Cooja radio trace, test log, and console log for each fixed seed.

See [COOJA_REPRODUCTION.md](COOJA_REPRODUCTION.md) for clean-machine setup and rebuild instructions.
